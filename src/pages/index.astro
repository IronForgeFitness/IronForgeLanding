<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iron Forge - Finally. A Workout App That Gets It.</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Inter:wght@400;500&display=swap" rel="stylesheet">
    <style>
        :root {
            --red: #E63946;
            --dark: #0a0a0a;
            --gray: #888;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--dark);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 60px 24px;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        /* Logo */
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 80px;
            opacity: 0;
            animation: fadeIn 0.6s ease forwards;
        }

        .logo img {
            width: 44px;
            height: auto;
        }

        .logo span {
            font-family: 'Bebas Neue', sans-serif;
            font-size: 1.4rem;
            letter-spacing: 0.1em;
        }

        /* Hero */
        .hero {
            margin-bottom: 80px;
        }

        .finally {
            font-family: 'Bebas Neue', sans-serif;
            font-size: clamp(3.5rem, 12vw, 7rem);
            line-height: 1.05;
            letter-spacing: 0.04em;
            opacity: 0;
            animation: slideUp 0.8s ease forwards 0.2s;
        }

        .finally .red {
            color: var(--red);
        }

        .tagline {
            font-size: 1.1rem;
            color: var(--gray);
            margin-top: 24px;
            max-width: 480px;
            line-height: 1.6;
            opacity: 0;
            animation: fadeIn 0.8s ease forwards 0.6s;
        }

        /* Pain Points */
        .pain-points {
            display: flex;
            flex-wrap: wrap;
            gap: 12px 24px;
            margin-bottom: 60px;
            opacity: 0;
            animation: fadeIn 0.8s ease forwards 0.8s;
        }

        .pain {
            font-size: 0.9rem;
            color: var(--gray);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .pain::before {
            content: '✗';
            color: var(--red);
            font-weight: 500;
        }

        /* Waitlist Form */
        .waitlist-form {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            max-width: 480px;
            opacity: 0;
            animation: fadeIn 0.8s ease forwards 1s;
        }

        .waitlist-form input[type="email"] {
            flex: 1;
            min-width: 200px;
            padding: 16px 20px;
            font-size: 1rem;
            font-family: 'Inter', sans-serif;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 8px;
            color: #fff;
            outline: none;
            transition: border-color 0.2s, background 0.2s;
        }

        .waitlist-form input[type="email"]::placeholder {
            color: var(--gray);
        }

        .waitlist-form input[type="email"]:focus {
            border-color: var(--red);
            background: rgba(255, 255, 255, 0.08);
        }

        .waitlist-form button {
            padding: 16px 32px;
            font-size: 0.95rem;
            font-weight: 500;
            font-family: 'Inter', sans-serif;
            background: var(--red);
            color: #fff;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: transform 0.2s, box-shadow 0.2s;
        }

        .waitlist-form button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(230, 57, 70, 0.3);
        }

        .waitlist-form button:disabled {
            opacity: 0.7;
            cursor: not-allowed;
            transform: none;
        }

        .form-note {
            width: 100%;
            font-size: 0.8rem;
            color: var(--gray);
            margin-top: 4px;
        }

        .form-success {
            padding: 16px 24px;
            background: rgba(230, 57, 70, 0.1);
            border: 1px solid var(--red);
            border-radius: 8px;
            color: #fff;
            font-size: 0.95rem;
        }

        /* Kit form overrides */
        .formkit-alert {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .formkit-alert:empty {
            display: none;
        }

        .formkit-alert-success {
            background: rgba(230, 57, 70, 0.15);
            border: 1px solid var(--red);
            border-radius: 8px;
            padding: 16px 24px;
            color: #fff;
            font-size: 1rem;
            margin-bottom: 12px;
        }

        .formkit-alert-error {
            background: rgba(255, 100, 100, 0.15);
            border: 1px solid #ff6b6b;
            border-radius: 8px;
            padding: 16px 24px;
            color: #ff6b6b;
            font-size: 0.9rem;
            margin-bottom: 12px;
        }

        /* Footer */
        .footer {
            margin-top: auto;
            padding-top: 80px;
            font-size: 0.85rem;
            color: var(--gray);
            opacity: 0;
            animation: fadeIn 0.8s ease forwards 1.2s;
            display: flex;
            align-items: center;
            gap: 16px;
        }

        .footer strong {
            color: #fff;
            font-weight: 500;
        }

        .instagram-link {
            color: var(--gray);
            transition: color 0.2s, transform 0.2s;
            display: flex;
            align-items: center;
        }

        .instagram-link:hover {
            color: var(--red);
            transform: scale(1.1);
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes slideUp {
            from { 
                opacity: 0; 
                transform: translateY(30px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        /* Mobile */
        @media (max-width: 600px) {
            .container {
                padding: 40px 20px;
            }

            .logo {
                margin-bottom: 60px;
            }

            .hero {
                margin-bottom: 60px;
            }

            .pain-points {
                flex-direction: column;
                gap: 8px;
            }

            .cta-section {
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Logo -->
        <div class="logo">
            <img src="/Logo.png" alt="Iron Forge Logo">
            <span>IRON FORGE</span>
        </div>

        <!-- Hero Statement -->
        <div class="hero">
            <h1 class="finally">
                FINALLY.<br>
                A WORKOUT APP<br>
                <span class="red">THAT GETS IT.</span>
            </h1>
            <p class="tagline">
                Built for lifters who use drop sets, supersets, and myo-reps. 
                No fluff. No fake AI. Just powerful tracking.
            </p>
        </div>

        <!-- Pain Points -->
        <div class="pain-points">
            <span class="pain">Drop sets</span>
            <span class="pain">Supersets</span>
            <span class="pain">Myo-reps</span>
            <span class="pain">Recovery tracking</span>
        </div>

        <!-- Waitlist Form - Kit Embed -->
        <script src="https://f.convertkit.com/ckjs/ck.5.js"></script>
        <form action="https://app.kit.com/forms/8869648/subscriptions" class="seva-form formkit-form" method="post" data-sv-form="8869648" data-uid="f1beae323b" data-format="inline" data-version="5" data-options='{"settings":{"after_subscribe":{"action":"message","success_message":"You're on the list! We'll notify you when Iron Forge launches."}}}' style="background-color: transparent;">
            <div data-style="clean">
                <ul class="formkit-alert formkit-alert-error" data-element="errors" data-group="alert"></ul>
                <div data-element="fields" data-stacked="false" class="seva-fields formkit-fields" style="display: flex; flex-wrap: wrap; gap: 12px; margin: 0;">
                    <div class="formkit-field" style="flex: 1; min-width: 200px; margin: 0;">
                        <input class="formkit-input" name="email_address" aria-label="Email Address" placeholder="Enter your email" required="" type="email" style="color: #fff; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.15); border-radius: 8px; font-weight: 400; padding: 16px 20px; width: 100%; font-size: 1rem; font-family: 'Inter', sans-serif;">
                    </div>
                    <button data-element="submit" class="formkit-submit" style="color: #fff; background-color: #E63946; border-radius: 8px; font-weight: 500; border: none; cursor: pointer; margin: 0;">
                        <div class="formkit-spinner"><div></div><div></div><div></div></div>
                        <span style="display: block; padding: 16px 32px;">Join Waitlist</span>
                    </button>
                </div>
            </div>
        </form>
        <p class="form-note">Launching soon on iOS. No spam, just launch updates.</p>

        <!-- Footer -->
        <div class="footer">
            <strong>Built by a lifter, for lifters.</strong> — Erik
            <a href="https://www.instagram.com/ironforge.app/" target="_blank" rel="noopener" class="instagram-link" aria-label="Follow on Instagram">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/>
                </svg>
            </a>
        </div>
    </div>
</body>
</html>
