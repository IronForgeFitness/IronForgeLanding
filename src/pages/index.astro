<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iron Forge - Coming Soon</title>
    <style>
        @import url('https://fonts.cdnfonts.com/css/coolvetica');
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-red: #B93838;
            --dark-bg: #0a0a0a;
            --medium-dark: #1a1a1a;
            --text-light: #e5e5e5;
            --text-muted: #999;
        }

        body {
            font-family: 'Inter', -apple-system, sans-serif;
            background: var(--dark-bg);
            color: var(--text-light);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow-x: hidden;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 50%, rgba(185, 56, 56, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 50%, rgba(185, 56, 56, 0.08) 0%, transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        .container {
            text-align: center;
            max-width: 900px;
            position: relative;
            z-index: 1;
            animation: fadeInUp 1.2s ease-out;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .logo-container {
            margin-bottom: 3rem;
            animation: fadeIn 1s ease-out 0.3s both;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .logo {
            width: 200px;
            height: auto;
            filter: drop-shadow(0 10px 30px rgba(185, 56, 56, 0.3));
            transition: transform 0.6s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        h1 {
            font-family: 'Coolvetica', 'Inter', sans-serif;
            font-size: 5rem;
            font-weight: 400;
            letter-spacing: 0.05em;
            margin-bottom: 1.5rem;
            color: var(--text-light);
            animation: fadeIn 1s ease-out 0.5s both;
        }

        .subtitle {
            font-size: 1.2rem;
            color: var(--text-muted);
            margin-bottom: 3rem;
            font-weight: 300;
            letter-spacing: 0.15em;
            text-transform: uppercase;
            animation: fadeIn 1s ease-out 0.7s both;
        }

        .divider {
            width: 60px;
            height: 2px;
            background: var(--primary-red);
            margin: 0 auto 3rem;
            animation: expandWidth 1s ease-out 0.9s both;
        }

        @keyframes expandWidth {
            from { width: 0; }
            to { width: 60px; }
        }

        .description {
            font-size: 1.15rem;
            color: var(--text-light);
            line-height: 1.8;
            margin-bottom: 3rem;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
            font-weight: 300;
            animation: fadeIn 1s ease-out 1.1s both;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 4rem;
            animation: fadeIn 1s ease-out 1.3s both;
        }

        .feature {
            padding: 2rem 1.5rem;
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 2px;
            transition: all 0.4s ease;
        }

        .feature:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(185, 56, 56, 0.3);
            transform: translateY(-5px);
        }

        .feature h3 {
            font-family: 'Coolvetica', 'Inter', sans-serif;
            font-size: 1.5rem;
            font-weight: 400;
            margin-bottom: 0.75rem;
            color: var(--primary-red);
        }

        .feature p {
            font-size: 0.95rem;
            color: var(--text-muted);
            line-height: 1.6;
            font-weight: 300;
        }

        .status {
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            padding: 1rem 2.5rem;
            background: rgba(185, 56, 56, 0.1);
            border: 1px solid rgba(185, 56, 56, 0.3);
            border-radius: 2px;
            font-size: 0.85rem;
            font-weight: 500;
            color: var(--primary-red);
            text-transform: uppercase;
            letter-spacing: 0.15em;
            animation: fadeIn 1s ease-out 1.5s both, pulse 3s ease-in-out 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { 
                box-shadow: 0 0 0 0 rgba(185, 56, 56, 0.4);
            }
            50% { 
                box-shadow: 0 0 0 10px rgba(185, 56, 56, 0);
            }
        }

        .status::before {
            content: '';
            width: 6px;
            height: 6px;
            background: var(--primary-red);
            border-radius: 50%;
            animation: blink 2s ease-in-out infinite;
        }

        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.3; }
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 3.5rem;
            }
            
            .logo {
                width: 150px;
            }

            .subtitle {
                font-size: 1rem;
            }

            .description {
                font-size: 1rem;
            }

            .features {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 2.5rem;
            }

            .logo {
                width: 120px;
            }

            .status {
                padding: 0.875rem 2rem;
                font-size: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo-container">
            <img src="Splash_NEU.png" alt="Iron Forge Logo" class="logo" />
        </div>
        
        <h1>IRON FORGE</h1>
        <p class="subtitle">Training Platform</p>
        
        <div class="divider"></div>
        
        <p class="description">
            A modern training platform for everyone who wants to track their 
            fitness goals systematically. Structured workouts, clear overview, 
            and full control over your training.
        </p>

        <div class="features">
            <div class="feature">
                <h3>Structured Workouts</h3>
                <p>Plan your training sessions with clear overview of exercises, sets, and reps.</p>
            </div>
            <div class="feature">
                <h3>Progress Tracking</h3>
                <p>Track your development and keep an overview of your performance.</p>
            </div>
            <div class="feature">
                <h3>Flexible Planning</h3>
                <p>Adapt your training to your daily routine – with full control over your schedule.</p>
            </div>
        </div>

        <div class="status">
            In Development
        </div>
    </div>
</body>
</html>